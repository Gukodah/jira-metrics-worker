[
  {
    "name": "MongoDBAccount",
    "type": "mongoDb",
    "data": {
      "user": "${MONGO_USER}",
      "password": "${MONGO_PASSWORD}",
      "database": "${MONGO_DB}",
      "host": "${MONGO_HOST}",
      "port": "${MONGO_PORT}",
      "options": "authSource=admin"
    }
  }
]